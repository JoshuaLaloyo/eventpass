EventPass agent handoff

Workspace: /home/danccodedev/Desktop/eventpass
Repo: https://github.com/JoshuaLaloyo/eventpass.git, branch master (local == origin, HEAD fb1339e)
Vercel project: eventpass-clhp, prod domain eventpass-clhp.vercel.app, Production Branch = master (confirmed correct)

Done:
- Git init, GitHub push, Vercel Git integration wired up.
- docker-compose.yml (postgres:16-alpine) + .env.local override for offline local dev. .env keeps Neon (prod) URL; .env.local gitignored.
- Vercel env vars DATABASE_URL + AUTH_SECRET were missing -> added; user must confirm Production scope is checked for both, and Preview/Development too (Preview deploys were failing on MissingSecret).
- organizer/events/new: two submit buttons ("Save as draft" -> /organizer, "Continue to add tickets" -> event page), via SubmitEvent.submitter.
- organizer/events/[id]: ticket-type table already auto-refreshes on add (no change needed).
- /scan page fully reworked (src/app/scan/page.tsx): camera view mounts via useEffect keyed on `running` (fixes 0x0 video bug from starting camera before container was visible); 5s "Verifying..." sweep animation -> green tick/red cross result overlay -> auto-resume; scan log now in its own always-visible card (persists after Stop) with admitted/rejected counts. CSS keyframes in src/app/globals.css (.animate-scan-sweep, .animate-pop-in).
- Removed stray wget-log from repo. NOTE: The.Bride.2026...mp4 (bogus 153-byte failed wget download) still sits untracked on disk, not committed, user hasn't said delete it.

Known loose end (was mid-fix when handed off):
- Vercel had several "Production"-tagged deployments that were just redeploys of the stale first commit (f1ebfee); real pushes (42dd03f, 063e848) landed as Preview. Fix in progress: filter Deployments list to "All", find latest Preview build, use its ... menu -> "Promote to Production". Confirm this actually landed before assuming prod is current.

IN PROGRESS / NEXT (what the user just asked for): Audit src/ for redundant/duplicate code and simplify BEFORE starting Flutterwave sandbox testing. Not started yet beyond listing files. Full file list is in src/app (pages + api routes), src/lib (auth.ts, prisma.ts, fulfill.ts, flutterwave.ts, email.ts, ticketPdf.ts, guards.ts, utils.ts), src/components/Nav.tsx. Worth checking for: repeated ugx/currency formatting, repeated fetch+load patterns across organizer pages, repeated auth/role-check boilerplate in api routes (requireRole already centralizes this in lib/auth.ts - verify all routes use it consistently), any dead exports.

After that cleanup, user wants to move to testing real Flutterwave sandbox payments: set PAYMENT_PROVIDER=flutterwave + FLW_SECRET_KEY + FLW_SECRET_HASH in .env (and Vercel), configure webhook URL in Flutterwave dashboard to https://<domain>/api/payments/webhook, remember Flutterwave can't reach localhost so this needs the Vercel deployment (or ngrok) working first. src/lib/flutterwave.ts + src/lib/fulfill.ts hold the processor-specific code, per README section 3.

Local resume commands:
  docker compose up -d && npm run dev
Git identity is already configured locally (user.name/user.email set by user directly, do not touch git config).
Pushing requires the user to run `git push origin master` themselves (personal access token needed interactively) - Claude's Bash tool cannot authenticate.
